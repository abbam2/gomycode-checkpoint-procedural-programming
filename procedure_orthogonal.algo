ALGORITHM orthogonal_vectors
VAR
    ps,i:integer
    v1,v2: array_of_real[100];        
BEGIN
    read(v1)
    read(v2)
    dot_product(ps)
    IF (ps:=0) THEN
        write("les vecteurs sont orthogonaux")
    ELSE
        write(ps)
    END_IF
END